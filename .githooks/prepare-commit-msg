#!/bin/sh
# Elimina atribución automática de Cursor en mensajes de commit
if [ -f "$1" ]; then
	sed -i '' '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$1"
	sed -i '' '/^Made-with: Cursor$/d' "$1"
fi
